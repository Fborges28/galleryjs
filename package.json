{
  "name": "gallery",
  "version": "1.0.0",
  "description": "image gallery - pure js",
  "main": "js/gallery.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch sass:styles",
    "server": "http-server -p 9090",
    "build": "browserify js/gallery.js -o js/bundle.js",
    "watch": "watchify js/gallery.js -o js/bundle.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jquery": "^3.3.1",
    "lodash": "^4.17.10",
    "resize-end": "^1.1.1",
    "save-dev": "^2.0.0"
  },
  "devDependencies": {
    "browserify": "^16.2.2",
    "watchify": "^3.11.0"
  }
}
